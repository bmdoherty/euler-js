const f = require('./51'); 

test('f', () => {
    expect( f(4) ).toEqual( 2 );
}); 

test('f', () => {
    expect( f(5) ).toEqual( 13 );
}); 

test('f', () => {
    expect( f(6) ).toEqual( 13 );
}); 

test('f', () => {
    expect( f(7) ).toEqual( 56003 );
}); 

test('f', () => {
    expect( f(8) ).toEqual( 121313 );
}); 

